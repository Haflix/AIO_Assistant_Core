TODO:
[] Validate plugin names and endpoint names: only letters, numbers, underscores. Blacklist reserved names ("system", "general").
[] Check endpoint access_names are unique across all loaded plugins (currently no uniqueness enforcement).
[] Remote execution: audit timeout propagation (remaining_time vs network latency).
[X] Notifiers (like MQTT works)